rootProject.name = 'hephaestus-engine'

[
    'api',
    'molang',
    'plugin',
    // adaption modules
    'adapt-v1_16_R3'
].forEach {
    include it
    // Add 'hephaestus' as prefix for all subprojects
    findProject(":$it")?.name = "hephaestus-$it"
}
